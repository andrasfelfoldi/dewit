import { StyleSheet } from 'react-native'
import { Metrics } from "../../Themes";

export default StyleSheet.create({
  container: {
    marginBottom: 5,
    marginTop: 5,
    borderBottomWidth: 1,
  },

  text: {
    fontSize: 18,
    
  }
})
